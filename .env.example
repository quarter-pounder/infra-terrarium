# AWS Credentials
# Option 1: Set these environment variables
# export AWS_ACCESS_KEY_ID="your-access-key"
# export AWS_SECRET_ACCESS_KEY="your-secret-key"
# export AWS_DEFAULT_REGION="ap-northeast-1"

# Option 2: Use AWS CLI profiles
# aws configure --profile terrarium

# Option 3: Use Terraform variables (not recommended for production)
# Create a terraform.tfvars file (gitignored):
# aws_access_key = "access-key"
# aws_secret_key = "secret-key"
# aws_region     = "ap-northeast-1"

# SSH Configuration
# Path to your SSH public key (default: ~/.ssh/terrarium-key.pub)
# ssh_public_key_path = "~/.ssh/terrarium-key.pub"

# EC2 Key Pair
# Name of the EC2 key pair in AWS (must exist in the target region)
# key_name = "infra_terrarium_core_linux_key"

# Instance Configuration
# name = "dev-linux-01"

# Network Configuration (optional - uses defaults if not specified)
# vpc_id            = "vpc-xxxxxxxxx"
# subnet_id         = "subnet-xxxxxxxxx"
# availability_zone = "ap-northeast-1a"
